# Every Japanese string the UI can display.
#
# generate.sh derives the font subset from this file, so a string that is
# not listed here will render as boxes on the device. When you change or add
# UI wording, add it here and re-run generate.sh.
#
# Lines starting with '#' and blank lines are ignored. ASCII is always
# included, so only the Japanese text matters.

# --- header ---
浦安市
更新
更新できず
未接続
接続中
同期中
取得中

# --- verdict banner ---
# Deliberately gentle: this thing sits in a home and talks to whoever walks
# past it, so it suggests rather than orders.
ほしてだいじょうぶ
みじかめならOK
そろそろとりこもう
あめ！とりこんで
じゅんびちゅう

# --- summary lines ---
さいだい
時間くらいでふりそう
いまふっています
時間あめのよほうはなさそう
あと
時間
レーダー

# --- status / errors ---
# These have to name the actual problem. A message that never changes tells
# the user nothing about whether the SSID is wrong, the password is wrong,
# or the access point is simply out of range.
Wi-Fiにつながりません
Wi-Fiにつないでいます
が見つかりません
パスワードが違うようです
電波が届いていないようです
SSIDが設定されていません
時刻をあわせています
よほうをとりよせています

# --- symbols the layout uses that ASCII does not cover ---
〜
／
：
！
☀
☁
☂
